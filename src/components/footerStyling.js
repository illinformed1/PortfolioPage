import { useSpring } from "react-spring";
import React from "react";

const SpringWrapper = () => {
  const openTabAni = useSpring({});
};

export default SpringWrapper;

/* I need tab open or not state. 

open a single tab by ID 

export d



function openTab(id) {


}


function(item) {
let ID tabs.findIndex(tab) in array 
openTab(ID)
}
*/
